# Expo Public API URL
# Set this to your deployed API base URL (must include the /api prefix)
#
# Examples:
#   Production:      https://ajitsir-api.onrender.com/api
#   Android emulator (local): http://10.0.2.2:4000/api
#   Real device (local):      http://192.168.x.x:4000/api  (replace with your LAN IP)
#
# The /api prefix is required — all backend routes are mounted under /api
# Confirmed in apps/api/src/index.ts (Phase 0 backend check)

EXPO_PUBLIC_API_URL=
